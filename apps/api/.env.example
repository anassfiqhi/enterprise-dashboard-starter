# Server Configuration
PORT=3001

# CORS Configuration
# Allowed origins for CORS (comma-separated for multiple origins)
CORS_ORIGIN=http://localhost:3000

# Better Auth Configuration
# Database connection string for Better Auth (PostgreSQL)
# Format: postgresql://user:password@host:port/database
BETTER_AUTH_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/enterprise_dashboard

# Better Auth Secret (generate with: openssl rand -base64 32)
BETTER_AUTH_SECRET=your-secret-key-here-change-in-production

# Better Auth URL (where your auth endpoints are hosted)
BETTER_AUTH_URL=http://localhost:3001/api/auth

# OAuth Providers (optional - uncomment and configure as needed)
# GITHUB_CLIENT_ID=your-github-client-id
# GITHUB_CLIENT_SECRET=your-github-client-secret
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret
