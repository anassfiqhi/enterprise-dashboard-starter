# API Configuration
# Base URL for the backend API
NEXT_PUBLIC_API_URL=http://localhost:3001

# Auth URL (defaults to API_URL/api/auth if not set)
NEXT_PUBLIC_AUTH_URL=http://localhost:3001/api/auth

# SSE Streaming URL (defaults to API_URL/api/v1/stream/events if not set)
NEXT_PUBLIC_SSE_URL=http://localhost:3001/api/v1/stream/events
